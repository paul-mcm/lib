# Echos $@ if $TestFlg true, else execs $@ in subshell
function runcmd {
    ${Trace:-false} && set -x
    typeset cmd="$@"
    ${TestFlg:-false} && echo "TEST: ${cmd[@]}" || ( ${cmd[@]} ) # Leave unquoted
    return $?
}
